{
  "name": "reentry-server",
  "version": "1.0.0",
  "description": "Server for Reentry space simulator",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && cp src/config/systemPrompt.xml dist/config/",
    "start": "node dist/index.js",
    "dev": "tsc -w & nodemon dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean-start": "rm -rf dist && npm run build && npm run start"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "openai": "^4.28.0",
    "socket.io": "^4.6.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.19",
    "nodemon": "^2.0.22",
    "typescript": "^5.3.3"
  }
}